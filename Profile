# ============================================
# KINVA MASTER - RENDER DEPLOYMENT
# ============================================

# Web App (Gunicorn)
web: gunicorn src.app:app --bind 0.0.0.0:$PORT --workers 4 --threads 2 --timeout 120 --access-logfile - --error-logfile -

# Telegram Bot Worker
worker: python src/bot.py
